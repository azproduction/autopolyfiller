#!/usr/bin/env node
new (require('../lib/cli'))({
    stdin: process.stdin,
    stdout: process.stdout,
    stderr: process.stderr,
    argv: process.argv,
    exit: process.exit,
    env: process.env
});
